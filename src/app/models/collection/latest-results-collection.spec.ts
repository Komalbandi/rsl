import { LatestResultsCollection } from './latest-results-collection';

describe('LatestResultsCollection', () => {
  it('should create an instance', () => {
    expect(new LatestResultsCollection()).toBeTruthy();
  });
});
