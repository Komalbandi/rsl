import { LatestResultsModel } from '../latest-results-model';

describe('LatestResultsModel', () => {
  it('should create an instance', () => {
    expect(new LatestResultsModel()).toBeTruthy();
  });
});
